pipeline {
    
    //Added agent configuration to use a specific Docker agent for the build.
     agent {
        label 'docker-agent01'
    }
    
    tools {
        // Ensure these tool names match your Jenkins global tool configuration
        maven 'maven3' 
        jdk 'jdk17'
    }
    
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        
        stage('Build') {
            steps {
                    sh 'mvn clean compile'
                }
        }
        
        stage('Test') {
            steps {
                sh 'mvn test'
            }
            post {
                always {
                    junit 'target/surefire-reports/*.xml'
                }
            }
        }
        
        stage('Package') {
            steps {
                sh 'mvn package -DskipTests'
            }
        }
        
        stage('Containerize') {
            steps {
                // Requires a Dockerfile at the repository root
                sh "podman build -tmy-springboot-app:${env.BUILD_ID} ."
            }
        }
    }
}